2. xss部分
xss的原理是什么? xss的三种类别有什么不同

xss存在于那些地方？仅仅是前端吗?
得看业务规模。如果攻击方绕过前端直接通过业务 API 提交攻击脚本到后端完成存储。
然后此时存储的内容用于多个下游业务端。下游的前端技术方案五花八门，有可能是现在流行的 SPA，也有可能是传统的 JSP，你如何保证下游在页面渲染时一定会过滤清洗输出内容，保障页面安全呢？。
通常需要保证入库的内容是安全可信的，所以仅靠前端防护是远远不够的。

xss用script就可以反射了吗?

存储型和反射型有什么区别?本质上的区别
反射性xss的前提是什么呢

xss的防护方法有哪些呢?要是有了httponly是不是就不能xss绕过了呢?
如果支持HttpOnly的浏览器检测到包含HttpOnly标志的cookie，并且客户端脚本代码尝试读取该cookie，则浏览器将返回一个空字符串作为结果。这会通过阻止恶意代码（通常是XSS）将数据发送到攻击者的网站来使攻击失败。
HttpOnly不是安全的 网上一搜有很多绕过方法
比如无论是否设置了HttpOnly 属性，phpinfo 函数都会输出当前请求上下文的Cookie信息。

在已经有shell的情况下，如何使用xss实现对目标站的长久控制


3. ssrf部分
讲一下ssrf的利用以及ssrf的成因
ssrf一般出现在哪里，你是如何判断这个点存在是ssrf漏洞的
能够对外发起网络请求的地方，就可能存在SSRF漏洞
云服务厂商：它会远程执行一些命令来判断网站是否存活等，所以如果可以捕获相应的信息，就可以进行SSRF测试
如何防御ssrf漏洞呢
ssrf有哪些绕过方法

4. csrf部分
讲一下csrf原理
csrf的防御方法有哪些? referer验证和token那个安全级别高呢?
refer更好 因为他更简单

